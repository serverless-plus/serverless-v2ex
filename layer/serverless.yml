org: serverless-v2ex
app: serverless-v2ex
stage: dev
component: layer
name: serverless-v2ex-layer

inputs:
  region: ${env:REGION}
  name: ${name}
  src: ../node_modules
  runtimes:
    - Nodejs10.15
    - Nodejs12.16
